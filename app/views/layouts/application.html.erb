<!DOCTYPE html>
<html>
<head>
  <title>What's cooking? - Restaurants near me</title>
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <link href="//fonts.googleapis.com/css?family=Andika&subset=latin" rel="stylesheet" type="text/css">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

</head>
<body>
  <div data-role="header">
    <% if action_name == "shortlistings" && @search %>
      <a href="<%= dinder_search_path(@search) %>" data-role="button" data-icon="arrow-l" class="ui-btn-left ui-btn-corner-all">Back</a>
    <% end %>
    <h1>Dinder</h1>
    <% if @search && action_name != "shortlistings" %>
      <a href="<%= shortlistings_dinder_search_path(@search) %>" data-role="button" data-icon="star" class="ui-btn-right ui-btn-corner-all" id="shortlist_count"><%= @search.shortlisted_restaurants.length %></a>
    <% end %>
  </div>


  <%= yield %>

</body>
</html>
